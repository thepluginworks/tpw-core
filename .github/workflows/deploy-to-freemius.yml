name: Deploy to Freemius

on:
  push:
    tags:
      - 'v*.*.*'  # Triggers on version tags like v1.0.0
  release:
    types:
      - published

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout plugin code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'release' && github.event.release.tag_name || github.ref }}

      - name: Set release metadata
        id: version
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            TAG_NAME="${{ github.event.release.tag_name }}"
          else
            TAG_NAME="${GITHUB_REF#refs/tags/}"
          fi

          echo "TAG_NAME=$TAG_NAME" >> $GITHUB_ENV
          echo "VERSION=${TAG_NAME#v}" >> $GITHUB_ENV

      - name: Build release package
        run: |
          ZIP_PATH="$RUNNER_TEMP/tpw-core-${VERSION}.zip"
          bash scripts/build-release-package.sh "$ZIP_PATH"
          echo "ZIP_PATH=$ZIP_PATH" >> $GITHUB_ENV

      - name: Upload package to GitHub release
        if: github.event_name == 'release'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "$TAG_NAME" "$ZIP_PATH" --clobber

      - name: Upload to Freemius
        if: github.event_name == 'push'
        env:
          FREEMIUS_DEV_ID: ${{ secrets.FREEMIUS_DEV_ID }}
          FREEMIUS_PUBLIC_KEY: ${{ secrets.FREEMIUS_PUBLIC_KEY }}
          FREEMIUS_SECRET_KEY: ${{ secrets.FREEMIUS_SECRET_KEY }}
          FREEMIUS_PLUGIN_ID: ${{ secrets.FREEMIUS_TPW_CORE_PLUGIN_ID }}
          FREEMIUS_RELEASE_MODE: released
        run: |
          echo "Freemius upload VERSION: $VERSION"
          echo "Freemius upload ZIP_PATH: $ZIP_PATH"

      - name: Download Freemius PHP SDK
        if: github.event_name == 'push'
        env:
          FREEMIUS_SDK_VERSION: 1.1.1
        run: |
          SDK_ARCHIVE="$RUNNER_TEMP/freemius-php-sdk-${FREEMIUS_SDK_VERSION}.tar.gz"
          SDK_DIR="$RUNNER_TEMP/freemius-php-sdk-${FREEMIUS_SDK_VERSION}"

          curl -fsSL \
            -o "$SDK_ARCHIVE" \
            "https://github.com/Freemius/freemius-php-sdk/archive/refs/tags/${FREEMIUS_SDK_VERSION}.tar.gz"

          mkdir -p "$SDK_DIR"
          tar -xzf "$SDK_ARCHIVE" --strip-components=1 -C "$SDK_DIR"

          echo "FREEMIUS_SDK_DIR=$SDK_DIR" >> $GITHUB_ENV

      - name: Upload to Freemius with PHP SDK
        if: github.event_name == 'push'
        env:
          FREEMIUS_DEV_ID: ${{ secrets.FREEMIUS_DEV_ID }}
          FREEMIUS_PUBLIC_KEY: ${{ secrets.FREEMIUS_PUBLIC_KEY }}
          FREEMIUS_SECRET_KEY: ${{ secrets.FREEMIUS_SECRET_KEY }}
          FREEMIUS_PLUGIN_ID: ${{ secrets.FREEMIUS_TPW_CORE_PLUGIN_ID }}
          FREEMIUS_RELEASE_MODE: released
        run: |
          php scripts/freemius-deploy.php
